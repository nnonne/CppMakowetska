#include <iostream>
int main()
{
    std::cout << "Enter numbers:" << std::endl;
    double a,b;
    std::cin >> a;
    std::cin >> b;
    if (abs(a) == abs(b)){
        std::cout << "absolute value of numbers are idential" << std::endl;
    }
    else if (abs(a) < abs(b)) {
        std::cout <<"absolute value of " << b << " is bigger" << std::endl;
        std::cout <<"absolute value of " << a << " is smaller" << std::endl;
    }
    else if (abs(a) > abs(b)) {
        std::cout <<"absolute value of " << a << " is bigger" << std::endl;
        std::cout <<"absolute value of " << b << " is smaller" << std::endl;
    }
    

    return 0;
}
